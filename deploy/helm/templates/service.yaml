apiVersion: v1
kind: Service
metadata:
  name: {{ include "soma-vault.fullname" . }}
  labels:
    {{- include "soma-vault.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "soma-vault.selectorLabels" . | nindent 4 }}
